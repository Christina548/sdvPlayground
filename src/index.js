let length = 3
let width = 5
function returnAnError(length) {
    if (isNaN(length))
    return 'Error'
}
function returnAnError2(width){
    if (isNaN(width))
    return 'Error'
}
fucntion area(length, width){
    return (length * width)
    console.log (area)
}
     
